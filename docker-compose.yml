version: "3"

services:
  app:
    build: .
    container_name: app
    restart: unless-stopped
    command: bash -c "cd app/ && python3 main.py"
    volumes:
      - .:/app
    ports:
      - "8000:8000"